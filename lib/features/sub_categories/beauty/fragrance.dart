// lib/features/sub_categories/pages/tvs_page.dart
import 'package:flutter/material.dart';

class Fragrance extends StatelessWidget {
  const Fragrance({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fragrance')),
      body: const Center(child: Text('Welcome to Fragrance')),
    );
  }
}

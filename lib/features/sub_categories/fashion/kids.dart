// lib/features/sub_categories/pages/tvs_page.dart
import 'package:flutter/material.dart';

class Kids extends StatelessWidget {
  const Kids({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Kids's fashion")),
      body: const Center(child: Text("Kids's fashion")),
    );
  }
}
